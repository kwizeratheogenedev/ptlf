{
  "name": "portfolio-app",
  "version": "1.0.0",
  "description": "IT Portfolio MERN Application",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd client && npm run build",
    "vercel-build": "cd backend && npm install && cd ../client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
